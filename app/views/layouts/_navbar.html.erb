<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center">
        <div class="flex-shrink-0 text-red-600">
          <span class="sr-only">Raro Food</span>
          <img src="/logo.png" alt="Logo da Raro Food" width="40">
        </div>
        <div class="hidden md:block">
          <div class="ml-4 flex items-baseline space-x-4">
            <%= link_to "Orders", orders_path, class: item_menu_classes %>
            <%= link_to "Chefs", chefs_path, class: item_menu_classes %>
            <%= link_to "Customers", customers_path, class: item_menu_classes %>
            <%= link_to "Dishes", dishes_path, class: item_menu_classes %>
            <%= link_to "Categories", categories_path, class: item_menu_classes %>
            <% if current_admin.root? %>
            <% end %>
          </div>
        </div>
      </div>
      <div class="hidden md:block">
        <div class="ml-4 flex items-center md:ml-6">

          <!-- Profile dropdown -->
          <div class="relative ml-3">
            <div>
              <button type="button" id="open-user-menu" class="flex max-w-xs items-center rounded-full bg-gray-800 text-sm focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
                <span class="sr-only">Open user menu</span>
                <img class="h-8 w-8 rounded-full" src="<%= current_admin.photo_url %>">
              </button>
            </div>
            <!--
                Dropdown menu, show/hide based on menu state.

                Entering: "transition ease-out duration-100"
                  From: "transform opacity-0 scale-95"
                  To: "transform opacity-100 scale-100"
                Leaving: "transition ease-in duration-75"
                  From: "transform opacity-100 scale-100"
                  To: "transform opacity-0 scale-95"
              -->
            <div class="context-menu hidden absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-md bg-white py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none" role="menu" aria-orientation="vertical" aria-labelledby="user-menu-button" tabindex="-1">
              <p class="block px-4 py-2 text-sm text-gray-700"><%= current_admin.name %>
                <% if current_admin.root? %>
                  Adminstrador
                <% end %>
              </p>
              <%= button_to "Log Out", destroy_admin_session_path, method: :delete, class: dropdown_menu_item, id: "user-menu-item-3" %>
            </div>
          </div>
        </div>
      </div>
      <div class="-mr-2 flex md:hidden">
        <!-- Mobile menu button -->
        <button type="button" class="inline-flex items-center justify-center rounded-md bg-gray-800 p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" aria-controls="mobile-menu" aria-expanded="false">
          <span class="sr-only">Abrir menu</span>
          <!-- Menu open: "hidden", Menu closed: "block" -->
          <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
          </svg>
          <!-- Menu open: "block", Menu closed: "hidden" -->
          <svg class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>
  <!-- Mobile menu, show/hide based on menu state. -->
  <div class="md:hidden" id="mobile-menu">
    <div class="space-y-1 px-2 pb-3 pt-2 sm:px-3">
      <%= link_to "Categories", categories_path, class: item_menu_classes %>
      <% if current_admin.root? %>
      <% end %>
    </div>
    <div class="border-t border-gray-700 pb-3 pt-4">
      <div class="flex items-center px-5">
        <div class="flex-shrink-0">
          <img class="h-10 w-10 rounded-full" src=<% current_admin.photo_url %>>
        </div>
        <div class="ml-3">
          <div class="text-base font-medium leading-none text-white"><%= current_admin.name %></div>
          <div class="text-sm font-medium leading-none text-gray-400"><%= current_admin.email %></div>
        </div>
        <button type="button" class="ml-auto flex-shrink-0 rounded-full bg-gray-800 p-1 text-gray-400 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800">
          <span class="sr-only">View notifications</span>
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M14.857 17.082a23.848 23.848 0 005.454-1.31A8.967 8.967 0 0118 9.75v-.7V9A6 6 0 006 9v.75a8.967 8.967 0 01-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 01-5.714 0m5.714 0a3 3 0 11-5.714 0" />
          </svg>
        </button>
      </div>
      <div class="mt-3 space-y-1 px-2">
        <% if current_admin.root? %>
          | Usuário Adminstrador
        <% end %>
      </p>
      <%= button_to "Log Out", destroy_admin_session_path, method: :delete, class: dropdown_menu_item, id: "user-menu-item-1" %>
    </div>
  </div>
</div>
<header class="bg-white shadow">
  <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8 flex justify-between items-center ">
    <h1 class="text-3xl font-bold tracking-tight text-gray-900 titulo-principal">
      <%= yield :title %>
    </h1>
    <span>
      <%= yield :new_button %>
    </span>
  </div>
</header>
</nav>

<%= javascript_include_tag "context-menu", "data-turbo-track": "reload", defer: true %>
